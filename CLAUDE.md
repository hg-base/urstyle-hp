# urstyle-hp ワークスペース

## ワークスペース分離ルール（最重要・絶対厳守）

このワークスペース（`C:\Users\hg-ba\urstyle-hp`）は完全に独立したプロジェクト空間です。

### 禁止事項
- **このディレクトリ外へのファイルアクセス・読み取り・書き込みは一切禁止**
- 他のワークスペース（auto-monetize 等）のファイルを参照・変更しない
- 親ディレクトリ（`C:\Users\hg-ba\`）以上への操作は禁止
- 他プロジェクトのデータベース・環境変数・シークレットへのアクセス禁止

### 作業範囲
- 全ての作業は `C:\Users\hg-ba\urstyle-hp\` 配下のみで完結させる
- 環境変数は `.env.local`（このディレクトリ内）から読み込む
- 出力ファイルはすべてこのディレクトリ内に保存する

---

## プロジェクト概要

**ユアスタイル合同会社のホームページ（HP）プロジェクト**

- ドメイン: urstyle-ent.com
- 会社名: ユアスタイル合同会社
- 代表者: 郡司 大輝
- 住所: 神奈川県横浜市港北区新横浜2丁目5-13-701
- メール: apps@urstyle-ent.com
- インボイス登録番号: T1021003016807

### 連絡先ルール
- **電話番号はいかなるページにも絶対に記載しない**
- 問い合わせはメールのみ: apps@urstyle-ent.com

---

## 技術スタック

- **フレームワーク:** Next.js (App Router)
- **スタイリング:** Tailwind CSS
- **デプロイ:** Vercel
- **言語:** TypeScript

## 開発ルール

1. **全ファイルはUTF-8で保存する**
2. **通貨は日本円（¥）で表示する**
3. **モバイルファースト設計（375px〜）**
4. **コメントは最小限（WHYが明白でない場合のみ記述）**

## ディレクトリ構成

```
urstyle-hp/
├── CLAUDE.md          # このファイル
├── .env.local         # 環境変数（git管理外）
├── app/               # Next.js App Router
│   ├── layout.tsx
│   ├── page.tsx
│   └── ...
├── components/        # 共通コンポーネント
├── public/            # 静的ファイル
└── ...
```
